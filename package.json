{
  "name": "hana-temp-mail",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dry-run": "wrangler deploy --dry-run",
    "tail": "wrangler tail",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "db:migrate": "wrangler d1 execute temp-mail-db --file=./schema.sql --remote",
    "db:migrate:local": "wrangler d1 execute temp-mail-db --file=./schema.sql --local"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.28.2",
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.40",
    "@vitest/coverage-istanbul": "^3.2.4",
    "vitest": "^3.2.4",
    "wrangler": "^4.76.0"
  },
  "dependencies": {
    "hono": "^4.12.9",
    "postal-mime": "^2.7.4",
    "zod": "^4.3.6"
  }
}
