name = "hana-temp-mail"
main = "src/index.ts"
compatibility_date = "2024-03-23"

# Configure these via Cloudflare dashboard or environment variables
# DO NOT commit database_id to public repo
[vars]
# MAIL_DOMAIN = "your-domain.com"

[[d1_databases]]
binding = "DB"
database_name = "temp-mail-db"
# database_id = "YOUR_DATABASE_ID"  # Configure in dashboard or .env

[triggers]
crons = ["0 * * * *"]